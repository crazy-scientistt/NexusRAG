# Leave empty in production: the frontend then calls the API on its own origin,
# which Vercel rewrites to the Python function.
VITE_API_URL=

# NexusRAG has no login, so no auth provider keys are needed.
