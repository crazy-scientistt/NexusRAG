# Railway Nixpacks Configuration
# This file tells Railway to install system dependencies

[phases.setup]
aptPkgs = ["poppler-utils", "tesseract-ocr"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"